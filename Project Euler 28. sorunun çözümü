var a = 1;
var d = 2;
var sum = 1;

// i is the number of boxes of numbers surrounding 1

for(var i = 1; i <= 500; i++)
{

//There are 4 diagonals within each box
for (var x = 1; x <= 4; x++)
   {a = a + d
    sum += a}

d = d + 2
}

document.write("Total sum of the diagonals is: " + sum);
